{
  "name": "@servora/types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared DTOs / Zod schemas — the API contract shared across surfaces",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo \"(types) no lint yet\"",
    "build": "echo \"(types) nothing to build yet\""
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
